{
  "name": "focusapp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -R public/* dist/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0",
    "better-sqlite3": "7.6.2",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": "18.x"
  }
}
