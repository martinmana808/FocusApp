
{
  "name": "focusapp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -R public/* dist/"
  },
  "dependencies": {
    "better-sqlite3": "^8.4.0",
    "rss-parser": "^3.13.0"
  },
  "engines": { "node": "18.x" }
}

